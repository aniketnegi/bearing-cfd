#+TITLE: Conical-Journal Mesh Families
#+STARTUP: content
#+OPTIONS: toc:2 num:nil
#+PROPERTY: header-args :eval never

* Scope

The meshing stages between accepted CAD and a CFD case answer separate
questions:

- whether Gmsh/OpenCASCADE imports the native BREP without changing it;
- whether Gmsh can triangulate the imported boundary;
- whether the no-port film forms a periodic Hex8 reference;
- whether a bore-pressure inlet can use either mesh-aligned or conforming
  circular Hex8 topology;
- whether the resolved central feed forms one connected Prism6 volume with an
  open film/tube junction.

No file in this directory defines viscosity, rotation speed, cavitation,
turbulence, pressure boundary values, or a CFD solver.

* Pipeline map

| Stage | Input | What Gmsh creates | What Python creates | Principal result |
|-------+-------+-------------------+---------------------+------------------|
| [[file:brep-preflight.org][BREP preflight]] | native unsplit/zoned BREP | no mesh | validation records | trusted OCC import and conformal zones |
| [[file:surface-smoke.org][Surface smoke]] | fragmented millimetre BREP | 2D Tri3 boundary mesh | classification and audits | boundary-mesh feasibility, no volume cells |
| [[file:structured-hex-no-port.org][Structured Hex8]] | =params.json= | no generated mesh | all nodes, Hex8 and Quad4 connectivity | all-Hex no-feed regression baseline |
| [[file:structured-hex-surface-inlet.org][Surface-inlet Hex8]] | =params.json= | no generated mesh | inflated Hex8 film and pressure-patch partition | all-Hex reduced-domain hydrodynamic mesh |
| [[file:body-fitted-inlet.org][Circular O-grid Hex8]] | =params.json= | stores complete discrete mesh | body-fitted surface plus physical 3-D sweep | conforming feed and full-volume OQ gate |
| [[file:layered-prism-central-feed.org][Layered Prism6]] | =params.json= plus native unsplit BREP | 2D unwrapped Tri3 master only | all 3D nodes, Prism6 and boundary connectivity | full ported fluid mesh |

The distinction between “Gmsh stores a mesh” and “Gmsh generates a mesh” is
important.  The volume-mesh pipelines register already-complete NumPy arrays as
Gmsh discrete entities.  Neither calls =gmsh.model.mesh.generate(3)=.

* Invariants shared by the pipelines

** Geometry and coordinates

- The bearing axis is global =Z=.
- The model is full 360 degrees.  A diametral symmetry mesh is not a valid
  rotating-journal CFD replacement.
- CAD and analytic construction parameters are millimetres.
- Solver mesh coordinates are metres.  The factor =0.001= is applied exactly
  once and recorded in reports/manifests.
- Film thickness means same-=z= radial clearance along a bearing-centred ray,
  not shortest surface-normal distance.
- The circumferential coordinate starts at the =-Y= minimum-gap ray.  The
  final circumferential cell connects directly to index zero; no duplicate
  =2*pi= node or seam boundary is retained.

See [[file:../../bearing_cfd/bearings/conical_journal/meshing/no_port.py::def generate_mesh][generate_mesh]]
and [[file:../../bearing_cfd/bearings/conical_journal/meshing/central_feed.py::def build_prism_mesh][build_prism_mesh]].

** Trust and provenance

- Input paths, hashes, coordinate units, volumes, and bounding boxes are
  checked before downstream work.
- Entity tags and return order are never treated as semantic names.  Geometry,
  centre of mass, bounding boxes, support type, and adjacency classify bodies
  and faces.
- Validation failures raise and publish a failure report.  They do not relax
  OCC tolerances or silently substitute approximate geometry.
- Solver-eligible artifacts are staged and atomically replace the existing
  output directory only after all mandatory checks pass.

Stage-specific checks use
[[file:../../bearing_cfd/bearings/conical_journal/meshing/brep_preflight.py::def require][require]].
Shared staging and publication are implemented by
[[file:../../bearing_cfd/artifacts.py::def make_staging_directory][make_staging_directory]]
and [[file:../../bearing_cfd/artifacts.py::def publish_generation][publish_generation]].

* Master data models

| Type | Owner | Meaning |
|------+-------+---------|
| =CadReference= | BREP preflight | frozen CAD measurements used to classify and validate OCC entities |
| =VolumeRecord= / =SurfaceRecord= | BREP preflight | geometry-derived OCC inventory, independent of imported tag order |
| =MeshData= | no-port Hex8 | immutable canonical points, Hex8, boundary quads, logical indices, metrics and metadata |
| =MasterMesh= / =BodyFittedMesh= | circular O-grid Hex8 | immutable body-fitted surface and swept physical Hex8 volume |
| =MasterMesh= | ported Prism6 | immutable unwrapped =(u,z)= Tri3 mesh, disk mask, rim/seam/partition edges |
| =PrismMesh= | ported Prism6 | immutable SI points, Prism6, external faces, mouth faces, fields and provenance |

Source links:

- [[file:../../bearing_cfd/bearings/conical_journal/meshing/brep_preflight.py::class CadReference][CadReference]]
- [[file:../../bearing_cfd/bearings/conical_journal/meshing/brep_preflight.py::class SurfaceRecord][SurfaceRecord]]
- [[file:../../bearing_cfd/bearings/conical_journal/meshing/no_port.py::class MeshData][MeshData]]
- [[file:../../bearing_cfd/bearings/conical_journal/meshing/body_fitted_inlet.py::class MasterMesh][Body-fitted MasterMesh]]
- [[file:../../bearing_cfd/bearings/conical_journal/meshing/body_fitted_inlet.py::class BodyFittedMesh][BodyFittedMesh]]
- [[file:../../bearing_cfd/bearings/conical_journal/meshing/central_feed.py::class MasterMesh][MasterMesh]]
- [[file:../../bearing_cfd/bearings/conical_journal/meshing/central_feed.py::class PrismMesh][PrismMesh]]

Canonical mesh arrays are made read-only.  Their connectivity is one-based
because the same arrays are registered with Gmsh;
VTU writers subtract one when handing connectivity to =meshio=.

* Choosing the mesh

Use the no-port structured Hex8 mesh as the regular =theta=, =z=, and
through-gap regression reference.

Use the [[file:structured-hex-surface-inlet.org][surface-inlet Hex8 mesh]] when
the intended model applies supply pressure directly on the stationary bore and
must exclude feed-passage head, wall friction, and jet dynamics.  Its inlet is
a mesh-aligned Quad4 patch rather than a resolved cylindrical drilling.  It
retains structured periodic connectivity but uses locally clustered,
nonuniform theta/z coordinates around that patch.

For the Fluent comparison, use the
[[file:body-fitted-inlet.org][body-fitted circular O-grid mesh]].  It keeps
the surface-pressure-inlet model boundary but replaces
the staircase rim locally, sweeps 12 physical thickness layers, and applies
the requested standard Fluent-equivalent OQ 0.9 gate to the complete 3-D
volume before publication.

Use the layered Prism6 mesh for the actual central feed.  The circular mouth
is naturally represented by an unstructured Tri3 master mesh.  Sweeping that
master creates layered prisms without forcing a fragile all-Hex block topology
around a branching circular passage.  The no-port baseline is retained rather
than being weakened or repurposed.

Once a ported =mesh_arrays.npz= has passed, continue with
[[file:../interchange/index.org][Solver-neutral interchange]] to construct clean
MSH/CGNS files.  That stage transfers the existing mesh; it does not belong in
the mesh-generation choice itself.

* Implementation chains

| Pipeline | Function chain | Regression |
|----------+----------------+------------|
| BREP preflight | [[file:../../bearing_cfd/bearings/conical_journal/meshing/brep_preflight.py::def load_reference][load_reference]] → [[file:../../bearing_cfd/bearings/conical_journal/meshing/brep_preflight.py::def _run_occ_checks][_run_occ_checks]] | [[file:../../tests/conical_journal/test_brep_preflight.py::def test_native_brep_preflight][native BREP test]] |
| Surface smoke | [[file:../../bearing_cfd/bearings/conical_journal/meshing/surface_smoke.py::def _run_mesh][_run_mesh]] → [[file:../../bearing_cfd/bearings/conical_journal/meshing/surface_smoke.py::def validate_mesh][validate_mesh]] | [[file:../../tests/conical_journal/test_brep_preflight.py::def test_native_brep_preflight][surface-smoke integration]] |
| No-port Hex8 | [[file:../../bearing_cfd/bearings/conical_journal/meshing/no_port.py::def generate_mesh][generate_mesh]] → [[file:../../bearing_cfd/bearings/conical_journal/meshing/no_port.py::def validate_analytic_mesh][validate_analytic_mesh]] → [[file:../../bearing_cfd/bearings/conical_journal/meshing/no_port.py::def add_discrete_model][add_discrete_model]] | [[file:../../tests/conical_journal/test_no_port.py::def test_analytic_geometry_orientation_counts_and_periodic_seam][analytic/topology test]] |
| Surface-inlet Hex8 | [[file:../../bearing_cfd/bearings/conical_journal/meshing/surface_inlet.py::def build_surface_inlet_mesh][build_surface_inlet_mesh]] → [[file:../../bearing_cfd/bearings/conical_journal/meshing/surface_inlet.py::def validate_surface_inlet_mesh][validate_surface_inlet_mesh]] → [[file:../../bearing_cfd/bearings/conical_journal/meshing/surface_inlet.py::def add_discrete_model][add_discrete_model]] | [[file:../../tests/conical_journal/test_surface_inlet.py][surface-inlet regression]] |
| Circular O-grid Hex8 | [[file:../../bearing_cfd/bearings/conical_journal/meshing/body_fitted_inlet.py::def build_ogrid_master][build_ogrid_master]] → [[file:../../bearing_cfd/bearings/conical_journal/meshing/body_fitted_inlet.py::def sweep_master_mesh][sweep_master_mesh]] → [[file:../../bearing_cfd/bearings/conical_journal/meshing/body_fitted_inlet.py::def export_body_fitted_case][export_body_fitted_case]] | [[file:../../tests/conical_journal/test_body_fitted_inlet.py][body-fitted export regression]] |
| Ported Prism6 | [[file:../../bearing_cfd/bearings/conical_journal/meshing/central_feed.py::def rim_coordinates][rim_coordinates]] → [[file:../../bearing_cfd/bearings/conical_journal/meshing/central_feed.py::def build_master_mesh][build_master_mesh]] → [[file:../../bearing_cfd/bearings/conical_journal/meshing/central_feed.py::def build_prism_mesh][build_prism_mesh]] → [[file:../../bearing_cfd/bearings/conical_journal/meshing/central_feed.py::def validate_topology][validate_topology]] | [[file:../../tests/conical_journal/test_central_feed.py::def test_mouth_nodes_faces_and_continuous_journal][mouth/topology test]] |
